<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="Theme.AppCompat.Light.NoActionBar.FullScreen" parent="@style/AppTheme">
        <item name="windowNoTitle">true</item>
        <item name="windowActionBar">false</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowContentOverlay">@null</item>
    </style>

    <style name="MarsRover.Widget.FloatingActionButton" parent="Widget.Design.FloatingActionButton">
        <item name="backgroundTint">@color/colorAccent</item>
        <item name="borderWidth">0dp</item>
    </style>

    <style name="MarsRover.Widget.FloatingActionButton.SpeedDial">
        <item name="android:layout_marginRight">8dp</item>
        <item name="fabSize">mini</item>
    </style>

    <style name="Translucent">
        <item name="android:windowBackground">@color/speed_dial_background</item>
    </style>

    <style name="MarsRover.Widget.TextView" parent="@android:style/Widget.TextView"/>

    <style name="MarsRover.Widget.TextView.SpeedDial">
        <item name="android:textSize">@dimen/text_site_default</item>
        <item name="android:paddingLeft">@dimen/padding_size_medium</item>
        <item name="android:paddingRight">@dimen/padding_size_medium</item>
        <item name="android:background">@drawable/background_rounded_corners</item>
    </style>

</resources>
